//! Intentionally empty. This crate exists for its integration tests and the
//! helper binaries they spawn (see the crate-level comment in `Cargo.toml`);
//! the lib target is here only so tools that expect one (e.g. trybuild's
//! generated test project) can depend on the package.
